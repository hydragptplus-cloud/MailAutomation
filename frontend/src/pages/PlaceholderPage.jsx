export default function PlaceholderPage({title,description}){return <section><div className="page-heading"><div><h1>{title}</h1><p>{description}</p></div><button className="primary">New {title.replace(/s$/,"")}</button></div><div className="panel placeholder"><h2>{title} module</h2><p>The backend API and project structure are ready. Connect the final CRUD table, form, modal, and validation workflow here.</p></div></section>}
